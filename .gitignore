node_modules/
.env
*.log
frontend/node_modules/
public/app/

# Certificados digitais (NFC-e) — NUNCA versionar. Ficam só no servidor, fora do git.
certificados/
*.pfx
*.p12

# Backups do banco — ficam só no servidor, nunca versionados (arquivo grande, e tem
# dados sensíveis de cliente).
backups/
